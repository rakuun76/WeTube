mixin videoInfo(video)
    div
        h4
            a(href=`/videos/${video.id}`)=video.title
        ul
            li=video.user
            li #{video.views} #{(video.views === 1) ? "view" : "views"}
            li=video.createdAt