extends base

block content
    form(method="POST")
        label(for="title") 제목
        input(type="text" id="title" name="title" value=video.title required)
        input(type="submit" value="Save")